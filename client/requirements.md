## Packages
date-fns | Formatting dates for timelines and notes
lucide-react | Already in stack, but ensuring it's used for all icons

## Notes
- Strict dark mode interface (VC Intelligence tool style)
- "Plus Jakarta Sans" for UI, "IBM Plex Mono" for data-dense displays
- Data exported to CSV/JSON handled entirely client-side
- Keyboard shortcut '/' implemented for global search focus
- Enrich functionality uses POST /api/companies/:id/enrich
